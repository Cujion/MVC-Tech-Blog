<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- link to use bootstrap -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css">
  <script src="https://kit.fontawesome.com/c502137733.js"></script>
  <link rel="stylesheet" href="/css/style.css">
  <title>The Tech Dashboard</title>
</head>
{{!-- DASHBOARD LAYOUT --}}
<body>
  <header id="row">
    <nav class="navbar col-lg-12 d-flex justify-content-start py-4 bg-dark">
      {{#if loggedIn}}
      <button class="btn-light btn-lg mx-2 border-primary"><a href="/">Home</a></button>
      <button class="btn-light btn-lg mx-2 border-primary"><a href="/dashboard">Dashboard</a></button>
      <button class="btn-light btn-lg mx-2 border-primary" id="logout"><a href="/login">Logout</a></button>
      {{else}}
      <button class="btn-light btn-lg mx-2 border-primary"><a href="/">Home</a></button>
      <button class="btn-light btn-lg mx-2 border-primary mb-2"><a href="/dashboard">Dashboard</a></button>
      <button class="btn-light btn-lg mx-2 border-primary" id="login"><a href="/login">Login</a></button>
      {{/if}}
        <h1 class="ml-5 mt-2 text-primary"><a>{{#if loggedIn}}{{username}}'s{{/if}} Tech Blog Dashboard</h1>
    </nav>
  </header>
  <main class="min-vh-100 col-12 bg-primary p-4">
    {{{body}}}
  </main>
  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>